
 <nav class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a><%= image_tag "universitypic.PNG", :class => "img" %></a>
          </div>
          <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav snip1217 navbar-right">
              <% if current_page? (userhome_path) %>
           <li><%= link_to "Home", userhome_path, :class => "a li" %></li>
            <li class="a">Logged in as Student</li>
          <% elsif current_page? (classregis_path(current_user.id))%>
          <li><%= link_to "Home", userhome_path, :class => "a li" %></li>
            <li class="a">Logged in as Student</li>
            
          <% else %>
             <li><%= link_to "Home", adminhome_path, :class => "a li" %></li>
            <li class="a">Logged in as Admin....</li>
          <%end%>
         <li><%= link_to "Log Out", logout_path, :class => "a li dif" %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div><!--/.container-fluid -->
  </nav>
  
  
  