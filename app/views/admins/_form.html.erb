<%= bootstrap_form_for(@admin) do |f| %>
  <% if @admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
      <% @admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="row fr">
 	<div class="col-md-12">
    <h2>New Admin</h2>
		<hr class="colorgraph">
		<div class="form-group">
			<%= f.text_field :email, class: "form-control input-lg", tabindex: "1", hide_label: true, placeholder: "Email" %>
		</div>
		<div class="row">
		  <div class="col-xs-12 col-sm-6 col-md-6">
		<div class="form-group">
			<%= f.password_field :password, class: "form-control input-lg", tabindex: "2", hide_label: true, placeholder: "Password" %>
		</div>
		</div>
		<div class="col-xs-12 col-sm-6 col-md-6">
		<div class="form-group">
			<%= f.password_field :password_confirmation, class: "form-control input-lg", tabindex: "3", hide_label: true, placeholder: "Password Confirmation" %>
		</div>
		</div>
		</div>
		<hr class="colorgraph">
		<div class="row">
			<div class="col-md-3"></div>
			<div class="col-xs-12 col-md-6">
				<%= f.submit "Add New Admin", class: "btn btn-primary btn-block btn-lg", tabindex:"6"%>
			</div>
			<div class="col-md-3"></div>
			</div>
	</div>
</div>
<% end %>

